/*Print true or false for following statements
a.) If it's monday & it's raining*/

#include<stdio.h>
int main()
{
	int isMonday=0;
    int	isRaining=1;
	printf("%d", isMonday || isRaining);
	return 0;
}
